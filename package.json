{
  "name": "doodle-jump-usth",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24 <25" },
  "scripts": {
    "setup": "powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/setup.ps1",
    "dev": "node scripts/dev.mjs",
    "build": "npm --prefix frontend run build",
    "test": "node scripts/test.mjs"
  }
}
